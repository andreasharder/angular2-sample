{
  "name": "angular2-application",
  "version": "0.0.2",
  "scripts": {
    "tsc": "tsc -p src -w",
    "start": "live-server --open=src/index.html --port=8080 ./",
    "test": "live-server --open=src/unit-tests.html --port=9090 ./"
  },
  "dependencies": {
    "angular2": "2.0.0-alpha.46",
    "systemjs": "0.19.2"
  },
  "devDependencies": {
    "jasmine-core": "2.3.4",
    "live-server": "^0.8.2",
    "typescript": "^1.6.2"
  }
}
